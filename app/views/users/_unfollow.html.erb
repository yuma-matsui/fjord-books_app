<%= form_with(model: current_user.active_following.find_by(followed_id: user.id), html: { method: :delete }, local: false) do |f| %>
  <%= f.submit t('follow_button.unfollow'), class: 'btn btn-unfollow' %>
<% end %>
