var followBtn = document.getElementById("follow-btn-<%= @user.id %>");
var followersCount = document.getElementById("followers-count");

followBtn.innerHTML = "<%= j render('users/unfollow', user: @user) %>";

if (followersCount) {
  followersCount.textContent = <%= @user.followers.count %>
}
